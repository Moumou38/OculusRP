using UnityEngine;
using System.Collections;

public class Tutorial : MonoBehaviour {
	public Texture[] tabTexture ; 
	public static bool tuto; 
	public bool  seen; 

	// Use this for initialization
	void Start () {
		/*
		this.GetComponent<GUITexture>().texture = tabTexture [0]; 
		Constant.Pause (); GameObject.Find("Timer").GetComponent<Timer>().Pause ();
		tuto = true; seen = false;  */
	
	}
	
	// Update is called once per frame
	void Update () {
		/*if (tuto) {		if(Input.GetKeyDown(KeyCode.E)) { tuto = false; this.GetComponent<GUITexture> ().texture = null; }
						if (Input.GetKeyDown (KeyCode.LeftArrow))
								this.GetComponent<GUITexture> ().texture = tabTexture [0]; 
						if (Input.GetKeyDown (KeyCode.RightArrow))
								this.GetComponent<GUITexture> ().texture = tabTexture [1];	
				
		
				} else {
			if(!seen) { Constant.unPause (); GameObject.Find("Timer").GetComponent<Timer>().Resume (); seen = true; } */ 		
		
		}
							
						
	
				
		
		 


}
